# CORS map - проверка origin
map $http_origin $cors_origin {
    default "";
    "~^https://vk\.com$" $http_origin;
    "~^https://.*\.vk\.com$" $http_origin;
    "~^https://.*\.vk-apps\.com$" $http_origin;
    "~^https://.*\.pages\.vk-apps\.com$" $http_origin;
}

server {
    listen 80;
    server_name kybyshka-dev.ru;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl;
    server_name kybyshka-dev.ru;

    # Добавляем правильные MIME типы
    include /etc/nginx/mime.types;
    types {
        application/javascript js;
        text/css css;
        image/svg+xml svg;
        font/woff2 woff2;
    }

    ssl_certificate /etc/nginx/ssl/fullchain.pem;
    ssl_certificate_key /etc/nginx/ssl/privkey.pem;
    
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;
    
    add_header Strict-Transport-Security "max-age=31536000" always;

    # Десктопная версия (корневой путь)
    location / {
        root /var/www/frontend-desktop;
        index index.html;
        try_files $uri $uri/ /index.html;
        
        # Security для VK iframe
        add_header X-Frame-Options "ALLOW-FROM https://vk.com" always;
        add_header Content-Security-Policy "frame-ancestors https://vk.com https://*.vk.com https://*.vk.ru;" always;
    }

    # Не кешировать index.html для десктопа
    location = /index.html {
        root /var/www/frontend-desktop;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }

    # Мобильная версия (путь /mob)
    location /mob {
        alias /var/www/frontend-mobile;
        index index.html;
        try_files $uri $uri/index.html /mob/index.html =404;
        
        # Security для VK iframe
        add_header X-Frame-Options "ALLOW-FROM https://vk.com" always;
        add_header Content-Security-Policy "frame-ancestors https://vk.com https://*.vk.com https://*.vk.ru;" always;
    }

    # Assets для десктопной версии
    location ~* ^/assets/.*\.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        root /var/www/frontend-desktop;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Assets для мобильной версии
    location ~* ^/mob/assets/.*\.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        alias /var/www/frontend-mobile;
        rewrite ^/mob/(.*) /$1 break;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Не кешировать index.html для мобильной версии
    location = /mob/index.html {
        alias /var/www/frontend-mobile;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }

    location /static/ {
        alias /var/www/static/;
        expires 30d;
        add_header Cache-Control "public, no-transform";
    }

    location /media/ {
        alias /var/www/media/;
        expires 30d;
        add_header Cache-Control "public, no-transform";
    }

    location /api/ {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # CORS с динамической проверкой origin
        add_header 'Access-Control-Allow-Origin' $cors_origin always;
        add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS' always;
        add_header 'Access-Control-Allow-Headers' 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range,Authorization' always;
        add_header 'Access-Control-Allow-Credentials' 'true' always;
        
        if ($request_method = 'OPTIONS') {
            add_header 'Access-Control-Allow-Origin' $cors_origin always;
            add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS' always;
            add_header 'Access-Control-Allow-Headers' 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range,Authorization' always;
            add_header 'Access-Control-Allow-Credentials' 'true' always;
            add_header 'Access-Control-Max-Age' 1728000;
            add_header 'Content-Type' 'text/plain; charset=utf-8';
            add_header 'Content-Length' 0;
            return 204;
        }
    }

    location /admin {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
